# Supabase project (see README "Comptes et sauvegardes" / CLAUDE.md "Comptes (Supabase Auth)").
# Required for accounts and online saves — without them the app still works but stays local-only
# (no sign-in, nothing synced to server/).
VITE_SUPABASE_URL=https://yourproject.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key
